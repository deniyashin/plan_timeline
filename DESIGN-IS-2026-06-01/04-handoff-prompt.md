# 04 — Handoff Prompt

Скопируйте блок ниже целиком и выполните `/make-plan`:

---

````
/make-plan Редизайн визуальной системы plan_timeline.html. Текущий дизайн получил 16/30 по аудиту Дитера Рамса, критические провалы в принципах #3 (Aesthetic, 1/3), #4 (Understandable, 1/3), #6 (Honest, 1/3), #8 (Thorough, 1/3).

Вердикт (из 03-verdict.md):
> 16/30 — редизайн, но не с чистого листа: структура и информационная архитектура состоятельны, нужно пересобрать визуальную систему и исправить UX-честность.

Почему редизайн, а не доработка: четыре принципа из десяти набрали по 1, включая #3 Aesthetic и #4 Understandable — именно эти два определяют первое впечатление учредителей при согласовании документа. Несистемная типографика (17+ отклонений от объявленной шкалы) и 3 label→behavior несоответствия — не косметика, а структурные дефекты.

Первичный пользователь 1: учредитель/топ-менеджмент — читает и согласовывает план изменений 2026–2027.
Первичный пользователь 2: проектный офис — ведёт данные, редактирует, публикует.
Стек: Vanilla JS, без бандлера, GitHub Pages. Нет TypeScript, нет тестов.
Направление: современнее, продуманнее, единая стилистика, без ярких цветов.

Сохранить (НЕ трогать):
- Типографическая пара Fraunces + Geist — принцип #7 scored 3, долговечная (css/base.css:73, css/hero.css:57–64)
- Четырёхрежимная архитектура brief/detailed/projects/edit — правильно решает два разных пользователя (js/modules/modes.js:7–12)
- Таймлайн как организующий принцип — релевантен задаче
- Структура CSS-токен системы (переменные в css/base.css:3–48) — правильная архитектура
- Цветовое кодирование 5 направлений изменений (#8B2635/#1E6E7A/#B8750A/#2F5233/#2E3A8A) — информационно обоснованное
- Editorial hero-секция (kicker/title/lede) — правильная иерархия для документа на согласование

Внедрить в приоритетном порядке:

1. #3 Aesthetic — Унифицировать визуальную систему.
   Жёстко соблюдать: type 11/13/15/20/40px (или эквивалент), spacing 4/8/16/24/32/48px, radius 4/8/12px.
   Удалить все хардкодные отклонения в css/programs.css (17+ spacing отклонений), css/hero.css (font-size 9, 10, 17, 19px), css/statuses.css:38 (Material Blue #3F51B5/#E8EAF6/#C5CAE9 — полностью вне токен-системы).
   Заменить --accent: #2FC6F6 (яркий циан, css/base.css:12) на нейтральный тёплый тон из существующей палитры (например, производный от --ink-muted #6B778C).
   Сократить с 53 до ≤20 активных токенов, убрав дублирующие rgba-тени и внесистемные цвета.

2. #6 Honest — Исправить три label→behavior несоответствия.
   a) Кнопка «Выйти из редактора» (plan_timeline.html:246, handler plan_timeline.js:40–43) → переименовать в «Вернуться к просмотру».
   b) Штамп «Редакция документа» (plan_timeline.html:59, plan_timeline.js:27–29) — сейчас обновляется при каждом локальном сохранении, не только при публикации. Или переименовать в «Последнее изменение», или показывать только при наличии po-published-at (не po-doc-edited).
   c) Баннер режима редактирования (js/modules/modes.js:11) обещает «экспорт/импорт» которого нет — убрать упоминание.

3. #8 Thorough — Добавить недостающие состояния UI.
   a) Добавить [disabled] стили и .is-disabled класс (css/states.css — файл уже существует).
   b) Добавить :focus-visible правило потребляющее --focus-ring (css/base.css:44 — переменная объявлена, но ни одного правила не использует её в 8 из 15 прочитанных CSS-файлов).
   c) Конвертировать ошибку loadRemote() из toast-only в inline page-level состояние (js/modules/publish.js:165 — сейчас только showToast('Не удалось загрузить данные')).

4. #4 Understandable — Устранить жаргон и дублирование контролов.
   a) «Контур использования» → «Охват изменений» (plan_timeline.html:76 и html:160).
   b) «Якорь-учредитель» → «Инициатор (учредитель)» (config/static-config.js:51).
   c) «Требует решения» → «Требует уточнения» (plan_timeline.html:113 — согласовать с program-flags.js:54 где флаг называется «требует уточнения»).
   d) Слить два filter-reset: qv-reset «Снять выборку» (html:115) и filter-reset «Очистить фильтры» (html:163) в один унифицированный контрол.

5. #9 Environmentally friendly — Сетевая эффективность.
   a) Добавить defer ко всем 19 <script> тегам (plan_timeline.html:251–269 — сейчас блокируют парсинг).
   b) Убрать cache: 'no-cache' и timestamp query из data.json fetch (js/modules/publish.js или loadRemote) — сейчас документ перегружается при каждом открытии.

Вне скопа этого редизайна (не трогать):
- HTML-структуру программ и проектов (plan_timeline.html line 174 — монолитная строка с 212 проектами)
- Логику DnD, задач, публикации — только UI/стили
- n8n webhook и схему данных data.json
- Переход на ES modules / бандлер (отдельная задача)

Результаты плана:
- Обновлённые CSS-токены в css/base.css (единый источник правды для всех переменных)
- Пофайловый список изменений по каждому CSS-файлу с конкретными строками
- Переименования строк UI с file:line до и после
- Новые правила в css/states.css для disabled и :focus-visible
- Регрессионный чеклист для принципа #7 (long-lasting, scored 3) — подтвердить, что Fraunces/Geist пара и editorial иерархия не затронуты
- Регрессионный чеклист для цветового кодирования 5 направлений изменений

Антипаттерны для этого редизайна:
- Не менять структуру HTML и JS-логику под предлогом стилевых правок
- Не вводить новый технологический стек (CSS-in-JS, Tailwind) — только токен-система в CSS-переменных
- Не трогать принципы scored 3 (#7 Long-lasting) — Fraunces+Geist пара и editorial секция
- Не добавлять тёмную тему без явного запроса — сначала сделать одну светлую, но правильную
````
